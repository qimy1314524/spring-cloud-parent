server:
  port: 3366
spring:
  application:
    name: spring-cloud-config-client3366
  cloud:
    config:
      label: master
      name: config
      profile: dev
      uri: http://localhost:3344
  rabbitmq:
    username: guest
    password: guest
    host: 120.26.173.145
eureka:
  client:
    service-url:
      defaultZone: http://localhost:5000/eureka
management:
  endpoints:
    web:
      exposure:
        include: '*'
